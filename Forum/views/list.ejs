<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/main.css" rel="stylesheet">
    <title>list</title>
</head>
<body class="grey-bg">
    <div class="white-bg">
        <% 글목록.forEach((글) => { %>
            <div class="list-box">
                <h4><%= 글.title %></h4>
                <p><%= 글.content %></p> 
            </div>
        <% }); %>
    </div>
</body>
</html>
